# 《大王请留步》PixiJS 单文件可玩原型开发规格

> 本文档用于指导 Codex / Cursor / Copilot 生成一个完整的 `index.html`。产品定位是“微信小游戏风格的三国轻策略短局对战”，强调低门槛、强反馈和 3 分钟内完成一局。本版本是浏览器可玩原型，不是微信小游戏工程，也不要求接入登录、广告、支付、排行或社交能力。

---

## 一、交付目标

请以资深游戏前端工程师的身份，根据本文档生成一个可以直接游玩的 PvE 原型。玩家通过翻开相邻地块扩张领地，获取随机建筑和资源，在空地上建设经济、兵营与防御塔，最终摧毁敌方主城或在倒计时结束时取得优势。

### 1.1 必须交付

- 只生成一个完整的 `index.html`，CSS 和 JavaScript 全部内嵌。
- 使用纯 JavaScript（ES6+）和原生 DOM，不使用 TypeScript、框架或构建工具。
- 游戏画面使用 PixiJS v7 渲染，UI 使用 DOM + CSS。
- PixiJS 固定使用 CDN 版本：

```html
<script src="https://cdn.jsdelivr.net/npm/pixi.js@7.4.3/dist/pixi.min.js"></script>
```

- 无需本地安装依赖，但首次运行必须联网加载 PixiJS；断网不保证可运行。
- 使用简单几何图形和文字作为占位美术，并在相关代码处标注 `// TODO: 替换为正式美术资源`。
- 必须支持从准备倒计时、正式战斗到胜负结算，再到重新开始的完整闭环。
- 桌面和移动浏览器均可操作；画布随窗口尺寸自适应，地图始终完整可见。

### 1.2 本期不做

- 不做微信小游戏平台适配和发布包。
- 不做联网匹配、账号、存档、排行榜、分享、广告和付费。
- 不做建筑升级、科技树、手动控制士兵或多武将养成。
- 不做高级翻格。仅在配置中预留费用字段，不绑定右键或长按事件。
- 不加载外部图片、音频、字体或 JSON 资源。

---

## 二、产品体验与核心循环

### 2.1 游戏定位

- 题材：三国 Q 版。
- 类型：六边形沙盘 + 自动出兵 + 实时攻防。
- 模式：玩家对战 AI。
- 单局目标时长：约 2～3 分钟。
- 操作原则：玩家只负责探索和建造，士兵自动移动和战斗。

### 2.2 核心循环

```text
翻格扩张领地
    -> 获得空地、资源或随机建筑
    -> 建金矿积累经济
    -> 建兵营持续出兵
    -> 建防御塔保护前线
    -> 自动交战和占领
    -> 摧毁敌方主城或在倒计时结束时取得优势
```

### 2.3 对局阶段

游戏必须使用明确的阶段状态：

```javascript
"prepare" -> "battle" -> "result"
```

- `prepare`：3 秒，仅显示开场倒计时，不产金、不出兵、不运行 AI、不接受地图操作。
- `battle`：180 秒，经济、建造、出兵、战斗、AI 和倒计时全部运行。
- `result`：停止所有逻辑更新，只保留结算 UI 和“再来一局”按钮。

---

## 三、地图与数据模型

### 3.1 地图坐标

- 地图为 9 列 × 7 行的平顶六边形网格。
- 使用轴向坐标 `(q, r)`，有效范围为 `q = 0..8`、`r = 0..6`。
- 六边形外接圆半径 `size = 36px`。
- 地图采用轴向坐标的平行四边形边界；只有同时满足范围条件的坐标有效。

初始坐标固定如下：

```javascript
const INITIAL_LAYOUT = {
  playerCity: { q: 0, r: 6 },
  playerTerritory: [{ q: 1, r: 6 }, { q: 0, r: 5 }],
  enemyCity: { q: 8, r: 0 },
  enemyTerritory: [{ q: 7, r: 0 }, { q: 8, r: 1 }],
};
```

- 两座主城所在格和各自两块初始领地在开局时已经揭示。
- 两块初始领地均为空地（`building === null`），允许建造。
- 其余 57 格均为 `unknown`、`owner = "none"`。

### 3.2 地块、建筑分离

不要用单个 `type` 同时表示地块和建筑。每个地块使用以下结构：

```javascript
{
  q: 0,
  r: 0,
  revealed: false,
  owner: "none",          // "player" | "enemy" | "none"
  terrain: "plain",      // 当前版本只有 plain
  building: null,         // Building 对象或 null
  capture: {
    side: null,
    progress: 0,
  },
}
```

建筑类型统一为：

```text
gold_mine
barracks_spear
barracks_cavalry
barracks_shield
barracks_archer
tower_arrow
tower_cannon
general_camp
main_city
```

这样“空地”表示 `revealed === true && building === null`，不再把 `empty` 当作建筑类型。

### 3.3 初始建筑

- 玩家主城位于 `(0, 6)`，敌方主城位于 `(8, 0)`。
- 主城初始生命值均为 1000。
- 主城不可出售、不可占领、不可重建；生命归零立即结束对局。

---

## 四、翻格与探索

### 4.1 可翻格条件

一个 unknown 地块同时满足以下条件才可翻开：

1. 当前处于 `battle` 阶段。
2. 地块 `revealed === false`。
3. 至少与一块当前操作者拥有的已揭示地块相邻。
4. 当前操作者金币不少于 25。

玩家点击合法 unknown 地块后立即消耗 25 金币并结算结果。非法点击只显示原因，不扣金币。

### 4.2 翻格结果

翻格完成后：

- 地块设置为 `revealed = true`。
- 地块 `owner` 设置为翻格方。
- 如果结果为建筑，则直接在该格生成满生命建筑，不再收取建造费用。
- 如果结果为金币奖励，则该格保持空地，并向翻格方增加 50 金币，最终金币不超过上限。
- 如果结果为兵营，先均匀随机一种兵营类型，再生成对应建筑。
- 如果结果为武将据点，则生成 `general_camp` 并立即召唤该方关羽。

普通翻格概率必须合计为 100%：

| 结果 | 概率 | 结算 |
|---|---:|---|
| 空地 | 45% | 保持空地，可建造 |
| 金矿 | 15% | 直接生成金矿 |
| 随机兵营 | 15% | 四种兵营各 3.75% |
| 箭塔 | 10% | 直接生成箭塔 |
| 霹雳塔 | 5% | 直接生成霹雳塔 |
| 武将据点 | 5% | 生成据点并召唤关羽 |
| 金币奖励 | 5% | 空地，并获得 50 金币 |

### 4.3 随机实现

- 使用累计概率抽取，避免多个独立随机判断导致概率偏差。
- 概率值全部来自 `CONFIG.revealRates.normal`。
- 若因浮点误差未命中，兜底结果必须为 `empty`。

---

## 五、经济与建造

### 5.1 经济规则

- 玩家和 AI 各自拥有独立金币账户。
- 双方初始金币均为 100。
- `battle` 阶段双方基础产出均为 2 金币/秒。
- 每座仍存活且归属己方的金矿额外提供 5 金币/秒。
- 金币上限为 9999，超出部分舍弃。
- 使用浮点数累计金币，HUD 显示时向下取整。
- “总产金量”统计实际到账的基础产出、金矿产出和翻格金币奖励；因金币上限丢弃的部分不计入。
- 金矿被摧毁后立即停止产金。

### 5.2 主动建造

只有同时满足以下条件才能建造：

1. 当前处于 `battle` 阶段。
2. 地块已揭示、归属建造方、`building === null`。
3. 金币足够。

| 建筑 | 费用 | 生命 | 功能 |
|---|---:|---:|---|
| 金矿 | 50 | 200 | 每秒额外产出 5 金币 |
| 枪兵营 | 80 | 300 | 每 3 秒生成 1 个枪兵 |
| 骑兵营 | 80 | 300 | 每 3 秒生成 1 个骑兵 |
| 盾兵营 | 80 | 300 | 每 3 秒生成 1 个盾兵 |
| 弓兵营 | 80 | 300 | 每 3 秒生成 1 个弓兵 |
| 箭塔 | 60 | 250 | 单体伤害 15，范围 2 格，间隔 1 秒 |
| 霹雳塔 | 120 | 250 | AOE 伤害 30，范围 2.5 格，间隔 2 秒，爆炸半径 1 格 |

### 5.3 出兵规则

- 兵营完成建造或由翻格直接生成后，首次出兵倒计时从 3 秒开始。
- 单位在兵营所在格中心生成，然后自动寻找敌方主城。
- 每方最多同时存在 80 个普通士兵，武将不计入该上限。
- 达到上限时兵营暂停出兵，但保留已完成的出兵进度；出现空位后立即生成。
- 兵营被摧毁不会影响已经生成的单位。

### 5.4 建筑摧毁

- 建筑生命归零后移除，地块保持已揭示并变为空地。
- 除主城外，建筑被摧毁时地块所有权暂时仍属于防守方。
- 攻击方必须继续完成占领，地块所有权才会改变。
- 主城生命归零时直接结束对局，不进入占领流程。

---

## 六、单位、战斗与占领

### 6.1 普通兵种

统一使用 `archer` / “弓兵”，不要再混用“工兵”。

| 兵种 | 生命 | 攻击 | 移速（格/秒） | 攻击间隔 | 攻击距离 | 克制 |
|---|---:|---:|---:|---:|---:|---|
| 枪兵 | 80 | 12 | 1.5 | 1.0 秒 | 0.6 格 | 骑兵 |
| 骑兵 | 70 | 15 | 2.5 | 1.2 秒 | 0.6 格 | 盾兵 |
| 盾兵 | 150 | 8 | 1.0 | 1.5 秒 | 0.6 格 | 弓兵 |
| 弓兵 | 60 | 14 | 1.5 | 1.0 秒 | 2.0 格 | 枪兵 |

克制关系为：

```text
枪兵 -> 骑兵 -> 盾兵 -> 弓兵 -> 枪兵
```

普通单位攻击普通单位时，伤害倍率精确按以下规则计算：

```javascript
function getCounterMultiplier(attacker, target) {
  if (attacker.counters === target.type) return 1.5;
  if (target.counters === attacker.type) return 0.75;
  return 1.0;
}
```

- 克制只作用于普通士兵之间。
- 对武将和建筑攻击时倍率固定为 1.0。
- 最终伤害允许为小数，血条显示时向上取整当前生命。

### 6.2 自动移动与寻路

- 单位默认战略目标是敌方主城。
- 寻路使用六边形 A*，启发函数为六边形距离。
- `unknown` 地块不可通行，所有已揭示地块均可通行。
- 建筑绘制在格子中心，但不作为不可穿越的墙；单位进入攻击距离后会停下攻击敌方建筑。
- 地图揭示、建筑创建/摧毁、地块归属改变时增加 `mapVersion`；单位发现版本变化后重新寻路。
- 如果敌方主城当前不存在可达路径，单位移动到最接近主城的可达前线格等待，并在地图变化后重新寻路。
- 单位之间不做刚体碰撞，允许轻微重叠；渲染时给同格单位增加小幅环形偏移。

### 6.3 目标选择

普通单位每次更新按以下优先级选择目标：

1. 攻击距离内最近的敌方普通单位或武将。
2. 攻击距离内最近的敌方非主城建筑。
3. 攻击距离内的敌方主城。
4. 没有目标时继续沿路径移动。

- 同距离时选择当前生命值最低的目标；仍相同则选择创建时间更早的目标。
- 目标死亡、离开攻击距离或变为无效后立即重新选取。
- 普通攻击不误伤己方。

### 6.4 防御塔攻击

- 箭塔优先攻击范围内距离最近的敌方单位，不主动攻击建筑。
- 霹雳塔选择范围内距离最近的敌方单位作为爆炸中心。
- 霹雳塔对爆炸中心 1 格范围内的所有敌方单位造成 30 点伤害，不伤害己方，不攻击建筑。
- 没有合法目标时保留冷却完成状态；目标进入范围后可以立即攻击。
- 所有距离均使用六边形格距离，不使用像素距离作为玩法判定。

### 6.5 占领规则

只有“已揭示、不是主城、没有建筑、所有权不是己方”的地块可以被占领。

- 至少一个己方单位站在目标格中心 0.35 格范围内，且范围内没有敌方单位时，占领进度开始累计。
- 同一方多个单位不会加速，占领时间固定为 2 秒。
- 敌我双方同时存在时，地块进入争夺状态，占领进度暂停。
- 占领方全部离开或死亡时，占领进度立即清零。
- 占领完成后 `owner` 切换为占领方，进度清零，并计入该方“本局成功占领次数”。
- 单位不会尝试占领 unknown 地块；必须由玩家或 AI 先翻开。
- 自动移动单位进入路径上的第一个可占领地块时必须停下，完成占领后再继续向敌方主城移动。

---

## 七、武将系统

### 7.1 武将据点

- 翻格结果为 `general_camp` 时，直接生成满生命武将据点，生命值为 300。
- 据点不生产普通士兵，只负责召唤和复活关羽。
- 每方最多同时拥有一个存活的关羽。再次翻出武将据点时只增加复活点，不重复召唤存活武将。
- 关羽死亡后进入 15 秒复活倒计时。
- 倒计时结束时，从该方仍存活的任意武将据点复活；若没有据点，则保持待复活状态，直到重新获得据点。
- 武将据点被摧毁后按普通建筑处理。

### 7.2 关羽属性

关羽的生命和攻击约为枪兵的 3 倍，其他属性采用专属配置：

| 属性 | 数值 |
|---|---:|
| 生命 | 240 |
| 攻击 | 36 |
| 移速 | 1.5 格/秒 |
| 攻击间隔 | 1.0 秒 |
| 攻击距离 | 0.6 格 |
| 技能冷却 | 5 秒 |
| 技能伤害 | 50 |
| 技能半径 | 1.5 格 |

- 关羽使用与普通单位相同的移动和目标选择逻辑。
- 技能冷却完成且 1.5 格内至少存在一个敌方单位时自动释放。
- 技能以关羽自身为中心，只伤害敌方单位，不伤害建筑和己方。
- 技能释放后播放短暂圆环特效。

---

## 八、AI 对手

### 8.1 基本约束

- AI 与玩家使用完全相同的经济、翻格、建造、出兵、战斗和占领规则。
- AI 在 `battle` 阶段每 2 秒决策一次，每次最多执行一个翻格或建造动作。
- AI 不知道 unknown 地块的预设随机结果，不允许读取隐藏信息作弊。
- AI 不使用高级翻格。

### 8.2 名词定义

- 前线格：己方空地中，与敌方领地相邻或到敌方主城六边形距离最短的地块。
- 建筑数量：只统计当前仍存活且归属 AI 的对应建筑。
- 可建空地：已揭示、归属 AI、没有建筑的地块。

### 8.3 决策优先级

AI 从上到下检查，执行第一条满足条件的规则。“继续存钱”也是本轮决策结果，不再检查后续翻格规则：

1. 没有金矿、存在可建空地且金币 ≥ 50：建金矿。
2. 没有金矿、存在可建空地且金币 < 50：本轮不行动，继续存到 50。
3. 没有兵营、存在可建空地且金币 ≥ 80：随机建一种兵营。
4. 没有兵营、存在可建空地且金币 < 80：本轮不行动，继续存到 80。
5. 敌方单位进入 AI 主城 3 格范围，且金币 ≥ 60、存在前线空地、箭塔少于 2 座：建箭塔。
6. 金矿少于 2 座，且金币 ≥ 75、存在可建空地：建金矿；支付后仍为探索保留至少 25 金币。
7. 兵营少于 3 座，且金币 ≥ 105、存在可建空地：随机建一种兵营；支付后仍为探索保留至少 25 金币。
8. 箭塔少于 2 座，且金币 ≥ 85、存在前线空地：建箭塔；支付后仍为探索保留至少 25 金币。
9. 金币 ≥ 25 且存在与 AI 领地相邻的 unknown：翻开一个地块。
10. 以上均不满足：本轮不行动并继续存钱。

翻格候选按以下方式选择：

1. 优先选择能缩短已揭示区域到玩家主城距离的地块。
2. 同分时优先选择拥有更多 unknown 邻居的地块。
3. 仍同分时随机选择。

---

## 九、胜负与统计

### 9.1 立即胜负

- 玩家主城生命归零：玩家失败。
- AI 主城生命归零：玩家胜利。
- 如果同一逻辑帧内双方主城都归零，判定平局。

### 9.2 时间结算

180 秒归零后依次比较：

1. 当前领地格数，更多的一方获胜。
2. 若相同，比较存活建筑的剩余总生命值，更多的一方获胜；包含主城和武将据点。
3. 若仍相同，比较当前存活单位的剩余总生命值，更多的一方获胜。
4. 若仍相同，判定平局。

领地格数定义为当前 `owner` 等于该方的已揭示地块数量，包含主城和有建筑的地块。

### 9.3 结算统计

双方分别记录：

- `unitKills`：击杀普通士兵和武将的总数；建筑不计入。
- `buildingsDestroyed`：摧毁非主城建筑数。
- `captures`：本局成功完成占领的次数，不含翻格获得的领地。
- `territory`：结算时当前领地格数。
- `goldEarned`：本局实际到账的金币总量，包含基础产出、金矿和翻格奖励，不包含初始金币。

---

## 十、UI 与交互

### 10.1 页面布局

页面由以下 DOM 层组成：

```html
<div id="game-container"></div>
<div id="hud"></div>
<div id="bottom-bar"></div>
<div id="build-menu"></div>
<div id="toast"></div>
<div id="prepare-overlay"></div>
<div id="result-panel"></div>
```

- 游戏画布铺满可用视口。
- HUD 固定顶部，底部栏固定底部，不得遮挡主要地图区域。
- 地图根据 HUD 和底部栏剩余空间自动缩放并居中。
- 最小适配宽度 360px；窄屏时建造菜单允许滚动或换行。
- 禁止页面默认滚动和双指缩放导致布局跳动，但不阻止按钮正常点击。

### 10.2 HUD

顶部 HUD 显示：

```text
敌方：金币 / 领地 / 主城HP    02:15    我方：金币 / 领地 / 主城HP
```

- 准备阶段中央显示 `准备 3 / 2 / 1`。
- 战斗阶段显示 `MM:SS`，不得出现负数。
- 金币显示向下取整，主城生命显示向上取整。

### 10.3 地块交互

- 点击合法 unknown：立即普通翻格。
- 点击不合法 unknown：底部提示无法翻格的原因。
- 点击己方空地：打开建造菜单。
- 点击己方或敌方建筑：显示名称、阵营、当前/最大生命和功能。
- 点击敌方或中立空地：只显示信息，不打开建造菜单。
- 点击画布空白、关闭按钮或按 `Escape`：关闭建造菜单。
- 建造菜单中的金币不足项必须禁用，而不是只改变颜色。

### 10.4 画布操作

- 默认视角必须能完整看到 9×7 地图，因此拖拽和缩放不是必需功能。
- 桌面端支持鼠标悬停高亮；移动端不依赖 hover 才能操作。
- 点击判定必须把屏幕坐标转换为地图容器本地坐标，再调用 `pixelToAxial`。

### 10.5 结算面板

- 显示“胜利”“失败”或“平局”。
- 显示胜负原因：主城被摧毁、领地优势、建筑生命优势、单位生命优势或完全平局。
- 显示双方五项结算统计。
- 提供“再来一局”按钮，必须在不刷新页面的情况下彻底重置游戏。
- 底部栏另提供“重新开始”按钮，点击后使用浏览器确认框防止误触。

---

## 十一、PixiJS 渲染规范

### 11.1 图层顺序

使用独立 Container，顺序固定为：

```text
mapLayer
buildingLayer
unitLayer
healthBarLayer
effectLayer
```

- 地图层：归属颜色、unknown 遮罩、问号、悬停和选中效果。
- 建筑层：金矿为黄色圆形、兵营为带兵种文字的方形、塔为三角形、据点为圆环、主城为大六边形。
- 单位层：枪兵橙色、骑兵棕色、盾兵蓝色、弓兵绿色；玩家描边青绿，敌方描边红色。
- 武将使用更大的圆形、金色光环和“关”字。
- 血条层：只显示受伤单位和所有建筑的血条，减少视觉拥挤。
- 特效层：普通攻击连线、霹雳塔爆炸圈、关羽技能圈、占领进度条和飘字。

### 11.2 渲染性能

- 不要在每帧为每个对象重新 `new PIXI.Graphics()` 或 `new PIXI.Text()`。
- 每个实体创建一次显示对象，更新时只修改位置、可见性和必要的绘制内容。
- 实体销毁时调用 `destroy()` 并从对应数组和 Container 移除。
- 静态地图仅在地块状态变化时重绘，不要无条件每帧重绘全部六边形。
- 单位不超过 160 个，当前原型无需对象池。

### 11.3 六边形工具

必须使用以下坐标公式，并明确地图容器负责整体偏移和缩放：

```javascript
const HexUtils = {
  size: 36,

  axialToPixel(q, r) {
    return {
      x: this.size * 1.5 * q,
      y: this.size * (Math.sqrt(3) * r + Math.sqrt(3) / 2 * q),
    };
  },

  pixelToAxial(x, y) {
    const q = (2 / 3 * x) / this.size;
    const r = (-1 / 3 * x + Math.sqrt(3) / 3 * y) / this.size;
    return this.round(q, r);
  },

  round(q, r) {
    const s = -q - r;
    let rq = Math.round(q);
    let rr = Math.round(r);
    const rs = Math.round(s);
    const qDiff = Math.abs(rq - q);
    const rDiff = Math.abs(rr - r);
    const sDiff = Math.abs(rs - s);
    if (qDiff > rDiff && qDiff > sDiff) rq = -rr - rs;
    else if (rDiff > sDiff) rr = -rq - rs;
    return { q: rq, r: rr };
  },

  directions: [
    { q: 1, r: 0 }, { q: 1, r: -1 }, { q: 0, r: -1 },
    { q: -1, r: 0 }, { q: -1, r: 1 }, { q: 0, r: 1 },
  ],

  neighbors(q, r) {
    return this.directions.map(({ q: dq, r: dr }) => ({ q: q + dq, r: r + dr }));
  },

  distance(q1, r1, q2, r2) {
    return (
      Math.abs(q1 - q2) +
      Math.abs(q1 + r1 - q2 - r2) +
      Math.abs(r1 - r2)
    ) / 2;
  },

  drawHex(graphics, size, fillColor, lineColor, lineWidth = 2) {
    graphics.beginFill(fillColor);
    graphics.lineStyle(lineWidth, lineColor);
    graphics.moveTo(size, 0);
    for (let i = 1; i < 6; i += 1) {
      const angle = Math.PI / 3 * i;
      graphics.lineTo(size * Math.cos(angle), size * Math.sin(angle));
    }
    graphics.closePath();
    graphics.endFill();
  },
};
```

`drawHex` 只绘制以显示对象本地原点为中心的路径；调用方必须设置 `graphics.position`。不要传入未使用的 `x, y` 参数。

---

## 十二、配置表

所有可调数值必须集中在脚本顶部。业务代码不得重复写魔法数字。

```javascript
const CONFIG = {
  map: {
    cols: 9,
    rows: 7,
    hexSize: 36,
  },
  economy: {
    startGold: 100,
    baseIncome: 2,
    goldCap: 9999,
    mineIncome: 5,
  },
  reveal: {
    normalCost: 25,
    advancedCostReserved: 250,
    goldReward: 50,
  },
  revealRates: {
    normal: {
      empty: 0.45,
      gold_mine: 0.15,
      random_barracks: 0.15,
      tower_arrow: 0.10,
      tower_cannon: 0.05,
      general_camp: 0.05,
      gold_reward: 0.05,
    },
  },
  battle: {
    prepareTime: 3,
    duration: 180,
    captureTime: 2,
    captureRadius: 0.35,
    maxUnitsPerSide: 80,
  },
  buildings: {
    gold_mine: {
      name: "金矿", cost: 50, hp: 200, income: 5,
    },
    barracks_spear: {
      name: "枪兵营", cost: 80, hp: 300, spawnInterval: 3, unitType: "spear",
    },
    barracks_cavalry: {
      name: "骑兵营", cost: 80, hp: 300, spawnInterval: 3, unitType: "cavalry",
    },
    barracks_shield: {
      name: "盾兵营", cost: 80, hp: 300, spawnInterval: 3, unitType: "shield",
    },
    barracks_archer: {
      name: "弓兵营", cost: 80, hp: 300, spawnInterval: 3, unitType: "archer",
    },
    tower_arrow: {
      name: "箭塔", cost: 60, hp: 250, atk: 15, range: 2, atkInterval: 1,
    },
    tower_cannon: {
      name: "霹雳塔", cost: 120, hp: 250, atk: 30, range: 2.5,
      atkInterval: 2, aoeRadius: 1,
    },
    general_camp: {
      name: "武将据点", cost: null, hp: 300,
    },
    main_city: {
      name: "主城", cost: null, hp: 1000,
    },
  },
  units: {
    spear: {
      name: "枪兵", hp: 80, atk: 12, speed: 1.5,
      atkInterval: 1, range: 0.6, counters: "cavalry",
    },
    cavalry: {
      name: "骑兵", hp: 70, atk: 15, speed: 2.5,
      atkInterval: 1.2, range: 0.6, counters: "shield",
    },
    shield: {
      name: "盾兵", hp: 150, atk: 8, speed: 1,
      atkInterval: 1.5, range: 0.6, counters: "archer",
    },
    archer: {
      name: "弓兵", hp: 60, atk: 14, speed: 1.5,
      atkInterval: 1, range: 2, counters: "spear",
    },
  },
  general: {
    guanYu: {
      name: "关羽", hp: 240, atk: 36, speed: 1.5,
      atkInterval: 1, range: 0.6, reviveTime: 15,
      skill: { name: "青龙偃月", damage: 50, range: 1.5, cooldown: 5 },
    },
  },
  ai: {
    decisionInterval: 2,
    maxMines: 2,
    maxBarracks: 3,
    maxArrowTowers: 2,
    emergencyCityRange: 3,
  },
};
```

初始化时必须校验普通翻格概率之和，允许误差不超过 `0.000001`；超出时在控制台抛出清晰错误并停止初始化。

---

## 十三、代码组织与主循环

### 13.1 单文件模块顺序

```text
CONFIG / 常量
工具函数与 HexUtils
GameState
MapManager
EconomyManager
BuildingManager
UnitManager
PathFinder
BattleManager
AIController
GameRenderer
UIController
Game 主控制器
初始化入口
```

每个模块使用如下格式分隔：

```javascript
// ===== MapManager 地图管理 =====
```

### 13.2 主循环

统一由 PixiJS Ticker 驱动：

```javascript
function gameLoop(ticker) {
  const dt = Math.min(ticker.deltaMS / 1000, 0.1);

  game.update(dt);
  gameRenderer.update(dt);
  uiController.update();
}
```

`game.update(dt)` 内部必须按阶段执行：

```text
prepare：只更新准备倒计时
battle：更新战斗倒计时 -> 经济 -> 建筑 -> 胜负检查 -> 单位 -> 胜负检查 -> AI
result：不更新玩法逻辑
```

- 战斗倒计时归零时立即执行时间结算并返回，不再更新该帧经济或战斗逻辑。
- 建筑或单位更新后如主城被摧毁，立即切换为 `result` 并返回；同一帧后续模块不得继续行动、造成伤害或产金。
- 浏览器切到后台后单帧 `dt` 最大按 0.1 秒处理，避免大跳。
- 重新开始必须销毁旧实体、清空计时器和显示层，再重新初始化；不得重复绑定 DOM 事件。

---

## 十四、开发顺序

1. 创建 HTML、CSS、DOM 容器和 PixiJS Application。
2. 实现配置校验、游戏阶段和可彻底重置的 Game 主控制器。
3. 实现六边形坐标、固定初始地图和自适应居中。
4. 实现地块状态、悬停、选中、点击和翻格。
5. 实现双方经济、建造菜单和建筑创建/摧毁。
6. 实现四种兵营、单位生成和单位上限。
7. 实现六边形 A*、自动移动、目标选择和普通攻击。
8. 实现箭塔、霹雳塔、占领和相关特效。
9. 实现武将据点、关羽技能与复活。
10. 实现 AI 决策。
11. 实现倒计时、胜负判定、统计和结算面板。
12. 完成移动端适配、错误提示、重新开始和性能整理。

---

## 十五、验收标准

生成完成后必须逐项自检：

### 15.1 启动与布局

- 联网状态下双击 `index.html` 可直接启动，无控制台语法错误。
- 360×640、768×1024、1366×768 三种视口下地图和主要按钮均可见。
- 初始化后双方各有 100 金币、1 座主城、2 块空领地，其余地块为 unknown。

### 15.2 玩法

- 准备阶段不能操作、产金、出兵或运行 AI。
- 合法翻格恰好扣除 25 金币，非法翻格不扣金币。
- 翻出的建筑立即生效，主动建造正确扣费。
- 金矿正确产金，兵营每 3 秒出兵，达到单位上限后暂停。
- 单位能绕过 unknown 区域，并在地图变化后重新寻路。
- 普通单位、塔、关羽技能的目标和伤害符合规则。
- 建筑摧毁后地块不会自动归攻击方，必须完成 2 秒占领。
- AI 不读取 unknown 结果，每次决策最多执行一个动作。

### 15.3 胜负与重置

- 任意主城生命归零后立即结算。
- 180 秒结束后按领地、建筑生命、单位生命顺序判定，支持平局。
- 结算统计口径与本文一致。
- “再来一局”和“重新开始”不会残留旧单位、旧计时器、旧金币或重复事件。
- 连续重新开始至少 3 次后仍能正常完成一局。

### 15.4 代码质量

- 所有数值来自 `CONFIG`，没有散落的核心玩法魔法数字。
- 不引用未定义变量、函数或 DOM 元素。
- 不在每帧无条件重建全部 Graphics/Text。
- 最终只输出一个完整的 `index.html`，不要拆分文件，不要省略代码，不要用伪代码代替实现。

---

*文档版本：v3.0（PixiJS 单文件原型） | 修订日期：2026-08-15*
