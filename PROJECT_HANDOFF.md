# 《大王请留步》项目交接文档

更新日期：2026-08-17

## 1. 项目信息

- 本地目录：`E:\aaa\ai\kingStop`
- GitHub：`https://github.com/chcCode/game-kingStop.git`
- 默认分支：`main`
- 运行方式：联网状态下直接打开 `index.html`
- 渲染引擎：PixiJS 7.4.3，通过 jsDelivr CDN 加载
- 当前形态：浏览器可玩原型，不是微信小游戏发布工程

开始新任务前先执行：

```powershell
cd E:\aaa\ai\kingStop
git status --short --branch
git log -5 --oneline
```

## 2. 文件结构

### `index.html`

- 页面 DOM 结构
- 全部 CSS
- 顶部 HUD、底部操作栏、建造菜单
- 地图选择菜单
- 准备倒计时和结算面板
- 脚本加载顺序

```html
<script src="https://cdn.jsdelivr.net/npm/pixi.js@7.4.3/dist/pixi.min.js"></script>
<script src="config.js"></script>
<script src="game.js"></script>
```

### `config.js`

- 地图预设及出生点
- 地图颜色和视口参数
- 经济与翻格概率
- 建筑、普通兵种和武将属性
- 对局和 AI 参数
- 配置字段、单位和联动关系说明

### `game.js`

- 六边形轴向坐标工具
- 偏移矩形坐标转换
- A* 寻路
- 游戏阶段和主循环
- 经济、翻格和建造
- 单位、战斗和占领
- 武将与复活
- AI 决策
- 胜负判定和统计
- PixiJS 渲染
- DOM UI 控制

### `大王请留步_PixiJS单文件版_Codex提示词.md`

- 早期 v3.0 原型需求文档
- 仍以“单文件版”为主，与当前三文件结构和多地图功能不完全一致
- 后续应更新或另建一份当前架构说明，避免以旧文档覆盖现有实现

## 3. 当前对局流程

游戏阶段：

```text
menu -> prepare -> battle -> result
```

- `menu`：地图选择，游戏暂停
- `prepare`：3 秒准备阶段，不产金、不出兵、不运行 AI
- `battle`：180 秒正式战斗
- `result`：停止玩法更新并显示结算

已支持：

- 开局选择地图
- 游戏中暂停并重新选择地图
- 返回当前战斗
- 重新开始当前地图
- 结算后再来一局
- 关闭结算框
- `Escape` 关闭菜单或结算层

## 4. 当前地图

### 虎牢关

- 9x7，共 63 格
- `axialParallelogram` 斜向轮廓
- 六边格半径 36px

### 赤壁水寨

- 11x7，共 77 格
- `axialParallelogram` 斜向轮廓
- 六边格半径 32px

### 南北对垒

- 11x8，共 88 格
- `offsetRectangle` 横向长方形轮廓
- 敌方位于上方，玩家位于下方

### 南北长阵

- 9x16，共 144 格
- `offsetRectangle` 纵向长方形轮廓
- 敌我沿唯一中央列上下严格对称
- 敌方主城偏移坐标为 `(4, 0)`
- 玩家主城偏移坐标为 `(4, 15)`
- 双方初始领地分别位于同一行的第 3、5 列

地图系统支持：

- 独立行列数和六边格大小
- 独立出生点和初始领地
- 独立地块配色
- 独立视口缩放参数
- 斜向轴坐标轮廓
- 偏移坐标长方形轮廓
- 地图边界、重复出生点和视口参数校验

## 5. 已完成玩法

### 翻格

- 只能翻开与己方领地相邻的未知地块
- 普通翻格消耗 25 金币
- 重复点击同一已翻开地块不会重复扣费
- 非法地块或金币不足时不扣费
- 翻格概率总和在启动时校验为 1

概率结果：

| 结果 | 概率 |
|---|---:|
| 空地 | 45% |
| 金矿 | 15% |
| 随机兵营 | 15% |
| 箭塔 | 10% |
| 霹雳塔 | 5% |
| 武将据点 | 5% |
| 50 金币 | 5% |

翻出的建筑直接生成，不再收取主动建造费。

### 经济

- 双方初始金币 100
- 基础产出 2 金币/秒
- 每座存活金矿产出 5 金币/秒
- 金币上限 9999
- HUD 向下取整显示金币
- 记录实际到账的总产金量

目前只在以下操作中扣金币：

1. 主动翻格
2. 主动建造

没有出兵费、维护费、攻击费、技能费、复活费或占领费。

### 建筑

| 建筑 | 费用 | 生命 |
|---|---:|---:|
| 金矿 | 50 | 200 |
| 四种兵营 | 80 | 300 |
| 箭塔 | 60 | 250 |
| 霹雳塔 | 120 | 250 |
| 武将据点 | 不可主动建造 | 300 |
| 主城 | 不可主动建造 | 1000 |

- 兵营每 3 秒自动出兵
- 箭塔攻击最近敌方单位
- 霹雳塔造成范围伤害
- 普通建筑摧毁后地块仍暂属防守方，需要继续占领
- 主城摧毁后立即结算

### 普通兵种

- 枪兵
- 骑兵
- 盾兵
- 弓兵

克制关系：

```text
枪兵 -> 骑兵 -> 盾兵 -> 弓兵 -> 枪兵
```

- 克制目标伤害 x1.5
- 被目标克制伤害 x0.75
- 其他情况 x1.0
- 克制只作用于普通士兵之间
- 每方普通单位上限 80，武将不计入

### 自动移动和寻路

- 使用六边形 A*
- 启发函数为轴向六边形距离
- 未探索地块不可通行
- 地图变化后通过 `mapVersion` 触发重新寻路
- 无法到达主城时，单位前往当前可达的最近前线
- 单位自动选择敌方单位、建筑和主城

### 占领

- 只能占领已探索、无建筑、非己方地块
- 占领时间 2 秒
- 多单位不加速
- 双方同时存在时暂停
- 占领方全部离开后进度清零
- 显示占领进度圆环

### 武将

当前武将为关羽：

- 生命 240
- 攻击 36
- 移速 1.5 格/秒
- 每 5 秒自动释放“青龙偃月”
- 技能伤害 50，半径 1.5 格
- 技能只攻击敌方单位
- 死亡后等待 15 秒
- 必须存在己方武将据点才能复活
- 每方最多一个存活关羽

### AI

- 每 2 秒决策一次
- 每次最多执行一个翻格或建造动作
- 不读取未知地块的随机结果
- 优先第一座金矿和第一座兵营
- 主城附近出现敌军时建箭塔
- 按配置补足金矿、兵营和箭塔
- 剩余金币用于继续翻格

已真正接入 AI 逻辑的配置：

- `decisionInterval`
- `maxMines`
- `maxBarracks`
- `maxArrowTowers`
- `emergencyCityRange`

### 胜负与结算

- 主城摧毁立即结束
- 同一逻辑帧双方主城同时摧毁则平局
- 时间结束后依次比较：
  1. 当前领地数
  2. 存活建筑总生命
  3. 存活单位总生命
  4. 仍相同则平局

结算数据：

- 击杀单位
- 摧毁建筑
- 成功占领
- 当前领地
- 总产金币

结算框支持：

- 再来一局
- 右上角关闭
- `Escape` 关闭
- 关闭后保持 `result` 状态，可使用底部重新开始或选择地图

## 6. UI 与渲染

DOM UI：

- 顶部双方金币、领地和主城生命
- 中央战斗倒计时
- 底部选择信息和操作提示
- 建造菜单
- 地图选择菜单
- Toast 提示
- 准备倒计时
- 结算面板

PixiJS 图层：

```text
map
building
unit
health
effect
```

当前仍使用几何占位美术：

- 六边形地块
- 建筑圆形、方形、三角形图标
- 单位圆点
- 关羽光环
- 血条
- 攻击连线
- AOE 圆环
- 占领进度

## 7. 已执行验证

开发过程中执行过：

- `node --check config.js`
- `node --check game.js`
- `git diff --check`
- 无头 Chrome 页面加载
- 360、390、430 等移动端宽度测试
- PixiJS Canvas 初始化
- 翻格和重复扣费逻辑检查
- 地图切换和重置
- 四张地图地块数量
- 南北地图出生点和镜像对称性
- 同帧双主城平局
- 结算框关闭并保持 `result` 状态

临时冒烟测试脚本验证后均已删除，仓库暂时没有长期保留的自动化测试。

每次修改后至少运行：

```powershell
node --check config.js
node --check game.js
git diff --check
```

涉及 UI、PixiJS 或地图时，还应使用浏览器实际打开 `index.html` 验证。

## 8. 已知限制和技术债

- 运行时依赖 PixiJS CDN，离线不能保证启动
- 尚未迁移为微信小游戏工程
- 没有正式美术、动画、音效和背景音乐
- 没有账号、存档、联网、排行榜、广告或支付
- 没有建筑升级、科技树、高级翻格和手动控制士兵
- `game.js` 较大，部分方法为超长单行，建议格式化和模块化
- `health` 图层已创建，但部分血条仍挂在实体容器中
- 需求文档仍以单文件版为主，与当前架构不完全一致
- 没有持续集成和正式自动化测试
- `economy.mineIncome` 与 `buildings.gold_mine.income` 存在重复配置语义
- AI 的部分建造储蓄阈值仍写在 `game.js` 中，未全部移入配置
- 当前数值只经过功能验证，尚未完成多局平衡测试

## 9. 推荐优化顺序

1. 更新需求文档，使其与三文件结构、四地图和地图选择系统一致。
2. 格式化并拆分 `game.js`，建议拆为地图、战斗、AI、渲染、UI 模块。
3. 统一重复配置，将 AI 储蓄阈值等魔法数字迁入 `config.js`。
4. 增加长期保留的自动化测试和基础 CI。
5. 增加调试面板、暂停和倍速，方便数值测试。
6. 让 AI 根据敌方兵种和地图形状选择兵营与进攻路线。
7. 完成多局自动模拟和 180 秒对局平衡。
8. 增加正式美术、单位动画、飘字、音效和背景音乐。
9. 如需发布微信小游戏，再进行运行环境和 DOM UI 迁移。

## 10. 新对话开场模板

复制下面内容开启新对话，并在最后填写具体任务：

```text
请继续优化项目 E:\aaa\ai\kingStop。

仓库：https://github.com/chcCode/game-kingStop.git
分支：main

请先执行：
1. git status --short --branch
2. git log -5 --oneline
3. 完整阅读 PROJECT_HANDOFF.md
4. 根据任务阅读 config.js、game.js 和 index.html 的相关部分

项目当前使用 PixiJS 7.4.3：
- index.html：页面、CSS 和脚本入口
- config.js：地图与全部玩法配置
- game.js：游戏逻辑、AI、渲染和 UI

已有功能包括：翻格、经济、建造、四兵种、A* 寻路、自动战斗、占领、箭塔、霹雳塔、关羽、AI、胜负结算、地图选择和四张地图。

四张地图：
- 虎牢关 9x7
- 赤壁水寨 11x7
- 南北对垒 11x8
- 南北长阵 9x16，敌我上下严格对称

请保留现有功能，不要回退无关改动。修改后至少运行：
- node --check config.js
- node --check game.js
- git diff --check

涉及页面、地图或 PixiJS 时，请再进行浏览器实际运行验证。

本次任务：<填写新的优化需求>
```

## 11. Git 工作约定

- 开始前检查工作区，避免覆盖用户改动
- 不使用 `git reset --hard` 或 `git checkout --` 清理用户文件
- 修改后先验证再提交
- 用户明确要求“上传”时再执行 `git push`
- 提交信息建议使用：
  - `feat:` 新功能
  - `fix:` 修复
  - `refactor:` 重构
  - `docs:` 文档
  - `test:` 测试
